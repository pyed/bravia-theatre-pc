name: Build & Release (BRAVIA Theatre PC)

on:
  push:
    branches: [ "main", "feature/*" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build & Test Native Windows App
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up .NET 9 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore BraviaTheatrePC.sln

      - name: Run unit tests
        run: dotnet test BraviaTheatrePC.sln --no-restore --verbosity normal

      - name: Publish standalone self-contained executable
        run: |
          dotnet publish src/BraviaTheatre.UI/BraviaTheatre.UI.csproj -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -p:DebugType=none -o publish

      - name: Publish lightweight framework-dependent executable
        run: |
          dotnet publish src/BraviaTheatre.UI/BraviaTheatre.UI.csproj -c Release --no-self-contained -p:PublishSingleFile=true -p:DebugType=none -o publish-fd
          Move-Item -Path "publish-fd/BraviaTheatrePC.exe" -Destination "publish/BraviaTheatrePC-FrameworkDependent.exe"

      - name: Generate SHA-256 Checksums
        shell: pwsh
        run: |
          $hashStandalone = (Get-FileHash publish/BraviaTheatrePC.exe -Algorithm SHA256).Hash
          Set-Content -Path "publish/BraviaTheatrePC.exe.sha256" -Value "$hashStandalone  BraviaTheatrePC.exe"
          Write-Host "BraviaTheatrePC.exe SHA256: $hashStandalone"

          $hashFd = (Get-FileHash publish/BraviaTheatrePC-FrameworkDependent.exe -Algorithm SHA256).Hash
          Set-Content -Path "publish/BraviaTheatrePC-FrameworkDependent.exe.sha256" -Value "$hashFd  BraviaTheatrePC-FrameworkDependent.exe"
          Write-Host "BraviaTheatrePC-FrameworkDependent.exe SHA256: $hashFd"

      - name: Create GitHub Release & Upload Assets
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          name: BRAVIA Theatre PC ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            publish/BraviaTheatrePC.exe
            publish/BraviaTheatrePC.exe.sha256
            publish/BraviaTheatrePC-FrameworkDependent.exe
            publish/BraviaTheatrePC-FrameworkDependent.exe.sha256
